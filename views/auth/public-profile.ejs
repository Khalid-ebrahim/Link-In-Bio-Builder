<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.username %>’s Public Profile</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body id="public-profile-page">
  <div class="container">
    <h1 class="main-title username-title"><%= user.username %>’s Links</h1>

    <% if (links.length > 0) { %>
      <ul class="links-list">
        <% links.forEach(link => { %>
          <li class="link-item">
            <a class="btn link-btn" href="<%= link.url %>" target="_blank"><%= link.title %></a>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="no-links">No links yet.</p>
    <% } %>
  </div>
</body>
</html>
